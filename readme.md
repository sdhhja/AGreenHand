本周进度：
这周完成一些基础功能。最头疼的是代码里字段名老是对不上，实体类写的是 studentId，我写 Service 的时候顺手就打成了 userId，结果满屏都是红线报错，调了好久才发现是这种低级错误，现已全部改对。
另外还把项目的文件结构重新理了一遍，之前因为类名和文件名不一致老是跑不起来，现在能正常登录注册并提交报修申请了。

遇到的困难：
命名不统一：被 description 和 content 这两个词搞混了好几次，导致数据库死活存不进数据。
文件报错：文件名写成了 RepairController 但里面类名叫 Main，报了一堆错，重命名之后总算消停了。
数据库：搞定了 MyBatis-Plus 的基础配置，现在报修的数据能稳定存进表里了。

下周打算：
把维修员那一块的逻辑写完，现在只能提交报修，还没法改状态。再加点简单的提示，别让程序一报错就直接挂掉。完成下周应有的任务
